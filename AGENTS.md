# 项目规范（AGENTS.md）

本文件用于约束 AI Coding Agent（如 Codex）在项目中的行为规范，所有代码修改必须遵守以下规则。

========================
一、Git 提交规范（必须遵守）
========================

1. 提交格式（强制使用 Conventional Commits）

必须使用格式：
<type>(<scope>): <description>
或
<type>: <description>

2. type 类型：
feat：新增功能
fix：修复 bug
docs：文档修改
style：代码格式调整（不影响逻辑）
refactor：代码重构
perf：性能优化
test：测试相关
chore：构建/依赖/工具链变更
build：构建系统相关
ci：CI/CD 相关

3. scope（可选）
auth / user / api / db 等模块范围说明

示例：
feat(auth): 添加登录功能
fix(api): 修复空响应问题

4. description 规范
- 使用祈使句（添加/修复/优化/移除）
- 不要模糊描述（如 update / fix bug）
- 不要句号结尾
- 简洁明确

========================
二、提交示例
========================

正确：
feat: 添加用户登录功能
fix(auth): 修复 token 过期问题
refactor(api): 重构请求封装
docs: 更新接口文档
chore: 更新依赖

错误：
update code
fix bug
done
随便改了一下

========================
三、提交粒度要求
========================

- 一个 commit 只包含一个逻辑变更
- 禁止混合功能/修复/重构
- 每个 commit 必须可运行、可回滚

========================
四、提交前检查
========================

执行：
git status
git diff

确保：
- 代码可运行
- 测试通过（如存在）
- 无调试代码
- 无无关修改

========================
五、禁止行为
========================

- 禁止不规范 commit message
- 禁止多个逻辑混合提交
- 禁止提交未完成代码
- 禁止混入格式化噪音

========================
六、目标
========================

- 保持 Git history 清晰
- 支持自动生成 changelog
- 支持语义化版本
- 提升可维护性
