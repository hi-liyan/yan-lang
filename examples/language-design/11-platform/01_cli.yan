module examples.cli

import yan.platform.cli
import yan.platform.console

fn greet(name: string) -> unit {
  console.println("hello, {name}")
}

fn main() -> Result<int, cli.Error> {
  let name = cli.argument("name")?
  greet(name)

  Ok(0)
}
