module examples.modules.domain

public type TaskId = int

public struct Task {
  id: TaskId
  title: string
  completed: bool
}

public fn rename_task(task: Task, title: string) -> Task {
  Task {
    id: task.id
    title: title
    completed: task.completed
  }
}
