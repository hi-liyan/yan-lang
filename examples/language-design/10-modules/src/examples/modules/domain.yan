module examples.modules.domain

pub type TaskId = int

pub struct Task {
  id: TaskId
  title: string
  completed: bool
}

pub fn rename_task(task: Task, title: string) -> Task {
  Task {
    id: task.id
    title: title
    completed: task.completed
  }
}
