module examples.option

import yan.platform.console

fn display_name(name: Option<string>) -> string {
  match name {
    Some(value) => value
    None => "anonymous"
  }
}

fn main() -> unit {
  console.println(display_name(Some("Lin")))
}
